{
  "private": true,
  "name": "@introduction-react/user-management",
  "version": "1.0.0",
  "scripts": {
    "clean": "lerna clean",
    "bootstrap": "lerna bootstrap",
    "dev": "lerna run dev --stream --parallel",
    "build": "lerna run build --stream --parallel",
    "deploy": "lerna run deploy --stream"
  },
  "devDependencies": {
    "lerna": "^3.13.2"
  },
  "workspaces": [
    "packages/*"
  ]
}
