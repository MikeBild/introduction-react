{
  "name": "moodbox",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "test": "lerna run test --parallel --stream",
    "dev": "lerna run dev --parallel --stream",
    "build": "lerna run build --stream",
    "e2e": "lerna run e2e --parallel --stream"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.0.3"
  },
  "dependencies": {
    "chart.js": "^4.2.1",
    "react-chartjs-2": "^5.2.0"
  }
}
