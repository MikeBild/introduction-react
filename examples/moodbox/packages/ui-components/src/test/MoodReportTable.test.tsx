/* import { render } from "@testing-library/react";
import { describe, it, expect } from "vitest";
import { MoodReportTable } from "..";
import { MoodReportTableAll } from "../organisms/MoodReportTable";

describe("MoodReportTable", () => {
  it("Bar", () => {

    const props:MoodReportTableAll = {header:{['Day':""]}}
    render(
      <MoodReportTable props=props>
        <h4>Content</h4>
      </MoodReportTable>
    );
    expect(1).toEqual(1);
  });
});
 */